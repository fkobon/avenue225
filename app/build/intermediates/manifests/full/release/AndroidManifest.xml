<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.evoir.avenue225"
    android:versionCode="2"
    android:versionName="1.1" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="18" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- Permission: Allow application to write to SDCard -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name="net.evoir.utils.AppController"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" >

        <!-- Splash screen -->
        <activity
            android:name="net.evoir.avenue225.SplashScreen"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.Holo.NoActionBar.Fullscreen" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main activity -->
        <activity
            android:name="net.evoir.avenue225.MainActivity"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:label="@string/app_name" >
 <!-- handling Listview refresh issue -->

            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
        </activity>

        <!-- Search activity -->
        <activity android:name="net.evoir.avenue225.SearchResultsActivity" >
        </activity>

        <!-- Detail of the post -->

        <activity android:name="net.evoir.avenue225.PostDetailActivity" >
        </activity>

        <!-- Detail of the post -->

        <activity
            android:name="net.evoir.avenue225.SettingsActivity"
            android:theme="@android:style/Theme.Holo.Light" >
        </activity>

        <!-- Service -->
        <service android:name="net.evoir.utils.JsonService" />
        <service android:name="net.evoir.utils.NotificationService" />
        <!-- receiver -->
        <receiver android:name="net.evoir.utils.BootReceiver" >
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>